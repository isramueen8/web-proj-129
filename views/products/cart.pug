extends ../layout

block content 
    h1 Add products to Food Cart
    - var total=0
    table(class="table") 
        thead 
            tr  
                th Item Name
                th Item Price
                th Size
                th Quantity
                th Suggestions
        each item in cart
            - total += Number(item.price)
            tbody
                tr
                    td=item.name        
                    td=item.price
                    td=item.city
                    td=item.location
                    td=item.detail
                    td
                        a(href="cart/remove/"+item._id ,class="btn btn-danger") Remove
        tfoot
            tr
                th Total        
                th=total          